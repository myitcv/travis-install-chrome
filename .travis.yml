language: node_js
node_js:
  - "5.4.1"

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - google-chrome
    packages:
      - g++-4.8
      - lsb-base
      - google-chrome-beta

script: echo ok
